    /* .then((response) => {
      if (!response.ok) {
        throw new Error('La respuesta de la red no fue correcta')
        }
        return response.json()
        })
        .then((data) => {
          setPelis(data)
          })
          .catch((error) => {
            console.error('Error al obtener datos de la API:', error)
            setError(error.message)
            })
            }, [api_key])
            if (error) {
              return <div className="text-red-500 font-bold text-center mt-4">Error: {error}</div>
              }
          return Pelis
        */
        {/*
    <div>
         <ul>
         {peliTrailer.map((peliTrailer, index) => 
            peliTrailer.type === 'Trailer' && 
            peliTrailer.site === 'YouTube' && 
            peliTrailer.key ? (
                <li key={index}>
                <iframe
                title={`Trailer de la película - ${peliTrailer.name}`} // Agrega un título único
                width="560"
                height="315"
                src={`https://www.youtube.com/embed/${peliTrailer.key}?autoplay=0&controls=1`} // URL corregida
                className="w-full h-auto"
                allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                allowFullScreen
                />
                </li>
                ) : null
                )}
                </ul> 
                </div>
                */}